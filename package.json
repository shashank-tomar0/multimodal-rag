{
  "name": "multimodal-rag-agenta",
  "version": "1.0.0",
  "description": "Agenta - Multimodal RAG Engine with Groq VLM and FAISS",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ..",
    "backend": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
